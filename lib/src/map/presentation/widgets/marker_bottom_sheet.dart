import 'package:flutter/material.dart';

class MarkerBottomSheet extends StatelessWidget {
  const MarkerBottomSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
